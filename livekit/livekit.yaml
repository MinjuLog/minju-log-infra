port: 7880
log_level: info

redis:
  address: redis:6379
  # Replace with a strong secret before production deploy.
  password: redis1234

keys:
  # Replace with your production API key/secret pair.
  CHANGE_ME_API_KEY: devsecret

rtc:
  tcp_port: 7881
  port_range_start: 40000
  port_range_end: 40100
  # Enable external IP discovery for internet-facing deployment.
  use_external_ip: true
